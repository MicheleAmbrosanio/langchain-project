[project]
name = "langchain-project"
version = "0.1.0"
description = "Project for learning LangChain and LangGraph"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
