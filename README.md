# LangChain Project

Project for learning and experimenting with LangChain and LangGraph.

## Requirements

- Python 3.13+
- Virtual environment (`.venv`)

## Setup

The project uses a local Python virtual environment.

Activate it with:

```powershell
.\.venv\Scripts\Activate.ps1